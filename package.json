{
  "name": "autoprime-simulador",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "worker:types": "wrangler types --config cloudflare/autoprime-conversions-api/wrangler.jsonc",
    "worker:check": "wrangler deploy --dry-run --config cloudflare/autoprime-conversions-api/wrangler.jsonc",
    "worker:deploy": "wrangler deploy --config cloudflare/autoprime-conversions-api/wrangler.jsonc"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "wrangler": "^4.34.0"
  }
}
